## Example config for scripts/release.sh
## Copy to .release.env (repo root) or scripts/release.env and edit.

# Pin wxPython version (or 'latest')
# WX_VERSION=4.2.3

# Force source build of wx (rare)
# FORCE_WX_SOURCE=1

# GPG signing
# GPG_PRIVATE_KEY="$(cat your_private_key.asc)"
# GPG_KEY_ID=YOUR_KEY_ID
# GPG_PASSPHRASE=your_passphrase

